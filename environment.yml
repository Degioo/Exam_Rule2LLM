name: rule2llm
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
    - numpy>=1.24
    - pandas>=2.0
    - scikit-learn>=1.3
    - scipy>=1.11
    - matplotlib>=3.7
    - seaborn>=0.12
    - tqdm>=4.65
    - requests>=2.31
    - python-dotenv>=1.0
    - pydantic>=2.0
    - jsonschema>=4.21
    - jupyter>=1.0
    - notebook>=7.0
    - ipykernel>=6.0
    # openai SDK — optional, providers use raw requests by default
    # uncomment if you prefer the SDK:
    # - openai>=1.0
    # fasttext-wheel — large, optional (Rule 05A baseline only)
    # - fasttext-wheel>=0.9.2
    # - gensim>=4.3
